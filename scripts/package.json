{
  "name": "matchpulse-sync-script",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone script that syncs real football data into Firestore.",
  "scripts": {
    "sync": "node sync-football-data.js"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "firebase-admin": "^12.6.0"
  }
}
